version: '3.8'

services:
  whatsapp-service:
    build: ./whatsapp-service
    ports:
      - "8501:8501"
    env_file:
      - ./whatsapp-service/.env
    depends_on:
      - openai-service
      - db-service

  openai-service:
    build: ./openai-service
    ports:
      - "8502:8502"
    env_file:
      - ./openai-service/.env
    depends_on:
      - db-service

  db-service:
    build: ./db-service
    ports:
      - "8000:8000"
    env_file:
      - ./db-service/.env
